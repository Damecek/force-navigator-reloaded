name: CI
on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "web/**"
      - "webpack.config.js"
      - "lwc.config.json"
      - "package.json"
      - "package-lock.json"
      - "tests/**"
  pull_request:
    branches: [main]
    paths:
      - "src/**"
      - "web/**"
      - "webpack.config.js"
      - "lwc.config.json"
      - "package.json"
      - "package-lock.json"
      - "tests/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - run: npm test
      - run: npm run lint
      - run: npm run build
